# ---------------------------------------------------------------------------
# KV cache 다관점 평가 - 의존성
#
#   uv venv
#   uv pip install -r requirements.txt
#   uv run python scripts/prepare_sources.py
#
# 정확한 재현이 필요하면 잠금 파일을 만들어 커밋한다:
#   uv pip compile requirements.txt -o requirements.lock.txt
#   uv pip sync requirements.lock.txt
# ---------------------------------------------------------------------------

# --- [1] 출처 수집 (prepare_sources.py) -------------------------------------
# 이 블록만 있어도 다운로드·해시·쪽수 기록은 돌아간다.
requests>=2.32,<3
pypdf>=5.1,<7
beautifulsoup4>=4.12,<5
lxml>=5.2,<7                 # bs4 파서. 없으면 표준 html.parser로 대체됨

# --- [2] 청킹 / 임베딩 / 검색 -----------------------------------------------
# 청크 경계는 임베딩 모델과 같은 토크나이저로 세야 400토큰 기준이 맞는다.
# 범용 토크나이저를 쓰면 실제 입력 길이가 512토큰 한도를 넘을 수 있다.
transformers>=4.46,<5
sentence-transformers>=3.3,<6
torch>=2.5                   # CPU 빌드로 충분. GPU 불필요
numpy>=1.26,<3
rank-bm25>=0.2.2,<0.3        # BM25. dense 검색과 RRF로 결합

# --- [3] 실행 편의 ----------------------------------------------------------
tqdm>=4.66,<5
