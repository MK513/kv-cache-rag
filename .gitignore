# --- 내려받은 원문 -----------------------------------------------------------
# 재현 시 scripts/prepare_sources.py 로 다시 받아 해시를 대조한다.
# data/manifest.json 은 해시·분량 기록이므로 커밋한다.
data/raw/

# --- 인덱스 / 중간 산출물 ----------------------------------------------------
data/chunks/
data/index/
# data/web_cache/ 는 제외하지 않는다 — 이해관계자 웹 조회 스냅샷은 재현성을 위해 커밋한다.

# --- 비밀 / 산출물 -----------------------------------------------------------
.env
output/*.md
output/*.pdf
output/*.jsonl

# --- Python ------------------------------------------------------------------
__pycache__/
*.py[cod]
.venv/
venv/
*.egg-info/
.ipynb_checkpoints/

# --- 도구 / 에디터 -----------------------------------------------------------
.ruff_cache/
.mypy_cache/
.pytest_cache/
.vscode/
.idea/

# --- OS ----------------------------------------------------------------------
.DS_Store
Thumbs.db
