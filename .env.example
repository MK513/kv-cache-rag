OPENAI_API_KEY=
TAVILY_API_KEY=

# config/settings.yaml 의 llm.model 을 덮어쓴다.
# gpt-5.6-luna 가 호출되지 않거나 structured output(tool calling)을 지원하지 않으면
# 아래 주석을 풀어 fallback 으로 바꾼다.
# LLM_MODEL=gpt-4.1-mini

LANGSMITH_API_KEY=
LANGSMITH_TRACING=false
LANGSMITH_PROJECT=kv-cache-rag
